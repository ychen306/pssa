import lit.formats

config.name = 'vegen'
config.test_format = lit.formats.ShTest(True)
config.suffixes = ['.c', '.ll', '.test']
config.test_source_root = os.path.dirname(__file__)
config.test_exec_root = config.builddir + '/test'

config.substitutions.append(('%opt', 'opt -load=' + config.builddir+'/lib/libPSSA.so'))
config.substitutions.append(('%clang', 'clang -Xclang -load -Xclang '+config.builddir+'/lib/libPSSA.so'))
